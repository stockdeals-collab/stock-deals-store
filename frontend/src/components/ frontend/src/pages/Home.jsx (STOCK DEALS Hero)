import { useState, useEffect } from 'react';
import { ShoppingCart, Package, Truck } from 'lucide-react';

export default function Home() {
  const [products, setProducts] = useState([]);

  useEffect(() => {
    // Sample STOCK DEALS products
    setProducts([
      { id: 1, name: 'Gaming Headset', price: 29, orig: 49, stock: 47, image: 'https://images.unsplash.com/photo-1579710039144-85d56a954b9b?w=400' },
      { id: 2, name: 'Wireless Mouse', price: 19, orig: 35, stock: 123, image: 'https://images.unsplash.com/photo-1583394838336-acd977736f90?w=400' },
    ]);
  }, []);

  return (
    <div className="max-w-7xl mx-auto px-4 py-12">
      {/* 🔥 STOCK DEALS HERO */}
      <div className="text-center py-24 bg-gradient-to-r from-orange-500 via-red-500 to-yellow-500 text-white rounded-3xl mb-16 shadow-2xl">
        <h1 className="text-5xl md:text-7xl font-black mb-6 bg-gradient-to-r from-yellow-300 via-white to-orange-200 bg-clip-text text-transparent drop-shadow-2xl">
          STOCK DEALS
        </h1>
        <p className="text-xl md:text-2xl mb-8 max-w-3xl mx-auto font-semibold opacity-95">
          🔥 Massive clearance | Ready-to-ship | Worldwide delivery
        </p>
        <div className="grid grid-cols-3 gap-6 max-w-lg mx-auto text-xl font-bold">
          <div className="bg-white/20 p-4 rounded-2xl backdrop-blur-xl flex items-center space-x-2">
            <Truck size={24} /> <span>USA 2-4 days</span>
          </div>
          <div className="bg-white/20 p-4 rounded-2xl backdrop-blur-xl flex items-center space-x-2">
            <Truck size={24} /> <span>UK 3-5 days</span>
          </div>
          <div className="bg-white/20 p-4 rounded-2xl backdrop-blur-xl flex items-center space-x-2">
            <Package size={24} /> <span>Pakistan Ready</span>
          </div>
        </div>
      </div>

      {/* Products Grid */}
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {products.map(product => (
          <div key={product.id} className="group bg-white rounded-3xl shadow-xl hover:shadow-2xl transition-all duration-500 overflow-hidden hover:-translate-y-2">
            <div className="h-64 overflow-hidden">
              <img src={product.image} alt={product.name} className="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500" />
              <div className="absolute top-4 left-4 bg-red-500 text-white px-3 py-1 rounded-full text-sm font-bold">
                🔥 41% OFF
              </div>
            </div>
            <div className="p-6">
              <div className="flex items-center mb-3">
                <Package className="w-5 h-5 text-green-500 mr-2" />
                <span className="text-green-600 font-semibold text-sm">Ready to Ship</span>
              </div>
              <h3 className="font-bold text-xl mb-3 line-clamp-2">{product.name}</h3>
              <div className="flex items-center space-x-3 mb-6">
                <span className="text-3xl font-black text-orange-600">${product.price}</span>
                <span className="text-lg text-gray-500 line-through">${product.orig}</span>
              </div>
              <div className="flex items-center justify-between text-sm text-gray-600 mb-4">
                <span>Stock: {product.stock}</span>
                <button className="bg-gradient-to-r from-orange-500 to-red-500 text-white px-6 py-3 rounded-xl font-bold hover:from-orange-600 hover:to-red-600 transition-all">
                  Add to Cart
                </button>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}
